# ishkul
ishkul.org

# Development cycle
Any changes in web / backend
Run 
`./cmd/tag-and-push-image.sh`
And restart the ec2

Any changes in infra 
Run 
`terraform validate`
`terraform plan`
`terraform apply`
